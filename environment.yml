name: kyklos
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.20
  - heyoka.py>=5.0  # Correct conda package name
  - pytest>=7.0
  - pytest-cov
  - jupyter
  - matplotlib
  - plotly
  - pip
  - pip:
    - -e .
